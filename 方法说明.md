# 代码更新到GitHub仓库方法说明

本文档详细介绍如何将本地代码更新到GitHub仓库，包括基本流程、分支管理、代理设置等内容。

## 一、准备工作

### 1. 安装Git
- 下载地址：https://git-scm.com/downloads
- 安装完成后，打开命令行工具验证：`git --version`

### 2. 配置Git用户信息
```bash
# 设置用户名
git config --global user.name "Your Name"

# 设置邮箱
git config --global user.email "your.email@example.com"
```

### 3. 克隆仓库（如果没有本地仓库）
```bash
git clone https://github.com/your-username/your-repository.git
```

## 二、基本更新流程

### 1. 检查当前分支
```bash
git branch
```

### 2. 拉取最新代码（可选）
```bash
git pull origin branch-name
```

### 3. 进行代码修改
在本地编辑器中修改代码文件。

### 4. 查看更改状态
```bash
git status
```

### 5. 添加更改到暂存区
```bash
# 添加单个文件
git add file-name

# 添加所有更改
git add .
```

### 6. 提交更改
```bash
git commit -m "commit-message"
```

### 7. 推送更改到远程仓库
```bash
git push origin branch-name
```

## 三、分支管理

### 1. 创建新分支
```bash
git checkout -b new-branch-name
```

### 2. 切换分支
```bash
git checkout branch-name
```

### 3. 推送新分支到远程仓库
```bash
git push --set-upstream origin new-branch-name
```

### 4. 删除本地分支
```bash
git branch -d branch-name
```

### 5. 删除远程分支
```bash
git push origin --delete branch-name
```

## 四、使用代理推送（针对特殊网络环境）

### 1. 设置HTTP/HTTPS代理
```bash
# 设置HTTP代理
git config --global http.proxy http://127.0.0.1:port

# 设置HTTPS代理
git config --global https.proxy https://127.0.0.1:port
```

### 2. 取消代理设置
```bash
# 取消HTTP代理
git config --global --unset http.proxy

# 取消HTTPS代理
git config --global --unset https.proxy
```

### 3. 验证代理设置
```bash
git config --global --list
```

## 五、解决冲突

### 1. 冲突的表现
当推送代码时出现类似错误：
```
! [rejected]        branch-name -> branch-name (fetch first)
error: failed to push some refs to 'https://github.com/your-username/your-repository.git'
hint: Updates were rejected because the remote contains work that you do not
hint: have locally. This is usually caused by another repository pushing to
hint: the same ref. If you want to integrate the remote changes, use
hint: 'git pull' before pushing again.
hint: See the 'Note about fast-forwards' in 'git push --help' for details.
```

### 2. 解决冲突的步骤
1. 拉取远程代码：`git pull origin branch-name`
2. 在编辑器中解决冲突（查找包含`<<<<<<<`、`=======`、`>>>>>>>`的行）
3. 添加解决后的文件：`git add .`
4. 提交解决冲突的更改：`git commit -m "resolve conflicts"`
5. 再次推送：`git push origin branch-name`

## 六、常见问题及解决方案

### 1. 认证失败
- 检查Git用户信息是否正确：`git config --global --list`
- 确保GitHub仓库权限设置正确
- 考虑使用SSH密钥认证替代HTTPS密码认证

### 2. 连接超时
- 检查网络连接是否正常
- 尝试设置代理（参考第四部分）
- 检查GitHub服务器状态：https://www.githubstatus.com/

### 3. 权限问题
- 确保你的GitHub账户有仓库的读写权限
- 检查Token权限设置（如果使用Token认证）

### 4. 文件换行符问题
- Windows使用CRLF，Linux/Mac使用LF
- 可以通过以下命令统一换行符设置：
  ```bash
  # 自动转换换行符
git config --global core.autocrlf true
  ```

## 七、示例：完整的推送流程（带代理）

```bash
# 设置代理
git config --global http.proxy http://127.0.0.1:10809
git config --global https.proxy https://127.0.0.1:10809

# 检查当前分支
git branch

# 创建并切换到新分支
git checkout -b feature/new-feature

# 进行代码修改
# ...

# 查看更改
git status

# 添加并提交更改
git add .
git commit -m "feat: add new feature"

# 推送新分支到远程仓库
git push --set-upstream origin feature/new-feature
```

## 八、GitHub Token权限说明

如果使用Token认证，确保Token具有以下权限：
- `repo`：完全控制私有仓库
- 或至少包含：
  - `public_repo`：访问公共仓库
  - `repo_status`：访问提交状态
  - `repo_deployment`：访问部署状态

更多权限信息可参考GitHub官方文档：https://docs.github.com/en/authentication/keeping-your-account-and-data-secure/creating-a-personal-access-token

---

**注意**：本文档中的命令示例适用于Windows系统的PowerShell环境。在Linux/Mac系统中，命令格式基本相同，但路径分隔符使用`/`而不是`\`。